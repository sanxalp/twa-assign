## Quiz UI (Vite + React + TS)

### Setup

1. Install deps: `npm install`
2. Run dev server: `npm run dev` (Vite will show the local URL)
3. Optional checks: `npm run lint`, `npm run typecheck`, `npm run build`
